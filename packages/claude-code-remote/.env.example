# ═══════════════════════════════════════════════════════════════
# ASTRID CONFIGURATION
# ═══════════════════════════════════════════════════════════════

# Webhook secret from Astrid (Settings → Webhook Settings)
# This is used to verify incoming webhooks from Astrid
ASTRID_WEBHOOK_SECRET=your-webhook-secret-here

# Callback URL (Astrid's API endpoint)
# This is where we send status updates back to Astrid
ASTRID_CALLBACK_URL=https://astrid.cc/api/remote-servers/callback

# ═══════════════════════════════════════════════════════════════
# AI PROVIDER CONFIGURATION
# ═══════════════════════════════════════════════════════════════
# Configure one or more AI providers. The server will route tasks
# to the appropriate provider based on the agent email:
#   - claude@astrid.cc → Claude Code CLI
#   - openai@astrid.cc → OpenAI API
#   - gemini@astrid.cc → Gemini API

# ───────────────────────────────────────────────────────────────
# CLAUDE CODE (requires Claude CLI installed)
# ───────────────────────────────────────────────────────────────
# Model to use (defaults to claude-sonnet-4-20250514)
CLAUDE_MODEL=claude-sonnet-4-20250514

# Max turns per interaction (prevents runaway sessions)
CLAUDE_MAX_TURNS=10

# Session timeout in milliseconds (15 minutes default)
CLAUDE_TIMEOUT=900000

# ───────────────────────────────────────────────────────────────
# OPENAI (optional - set API key to enable)
# ───────────────────────────────────────────────────────────────
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Model to use (defaults to gpt-5)
OPENAI_MODEL=gpt-5

# Max iterations for tool use loop
OPENAI_MAX_ITERATIONS=50

# ───────────────────────────────────────────────────────────────
# GEMINI (optional - set API key to enable)
# ───────────────────────────────────────────────────────────────
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Model to use (defaults to gemini-3-pro)
GEMINI_MODEL=gemini-3-pro

# Max iterations for tool use loop
GEMINI_MAX_ITERATIONS=50

# ═══════════════════════════════════════════════════════════════
# SERVER CONFIGURATION
# ═══════════════════════════════════════════════════════════════

# Server port
PORT=3001

# Server host (0.0.0.0 for external access)
HOST=0.0.0.0

# ═══════════════════════════════════════════════════════════════
# SESSION MANAGEMENT
# ═══════════════════════════════════════════════════════════════

# Where to store session mappings
SESSION_MAP_PATH=./data/sessions.json

# ═══════════════════════════════════════════════════════════════
# PROJECT PATHS
# ═══════════════════════════════════════════════════════════════

# Default project path (if not mapped per list)
DEFAULT_PROJECT_PATH=/home/user/projects/default

# Per-list project paths (optional)
# Format: PROJECT_PATH_<LIST_ID_WITH_UNDERSCORES>=/path/to/project
# Example: PROJECT_PATH_ABC123_DEF456=/home/user/projects/my-app

# Or use a JSON file for project mappings
# PROJECT_MAP_PATH=./data/projects.json

# ═══════════════════════════════════════════════════════════════
# GITHUB CONFIGURATION (for PR creation)
# ═══════════════════════════════════════════════════════════════

# GitHub token for cloning repos and creating PRs
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=

# Directory to store cloned repositories
REPOS_DIR=./repos

# Data directory for sessions and configs
DATA_DIR=./data
